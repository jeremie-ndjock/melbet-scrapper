# Secrets
.env
.env.*
!.env.example

# Outils locaux
.playwright-mcp/
__pycache__/
*.pyc
*.bak

# Données de couverture (générées localement par `pytest --cov`, jamais versionnées)
.coverage
.coverage.*
htmlcov/

# Sauvegardes locales produites par scripts/backup_db.sh (des données, jamais du code)
/backups/

# État interne de l'outil (Claude Code), pas du code du projet — committé par erreur à l'étape 8
.claude/scheduled_tasks.lock

# Dépendance de build locale (docs/forecasting/generate.js, génération du plan d'entraînement)
node_modules/
package-lock.json
